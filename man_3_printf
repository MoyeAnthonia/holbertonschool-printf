.\"This is a manpage for _printf function
.TH printf_funcs_a 3 "November 2025" "1.0" "Program Manual"

.SH NAME
_printf \- custom implementation of the standard printf function.

.SH SYNOPSIS
.B #include "main.h"
.PP
.BI "int _printf(const char *format, ...);"

.SH DESCRIPTION
The 
.B _printf() is used to print out character and format specifiers according to the argument passed.
It uses the C variadic argument macros
.B va_start,
.B va_arg,
and
.B va_end
to process arguments supplied to the function.

.SH FORMAT SPECIFERS
The following conversion are used
.TP 
B. %c
Prints a single character

.TP 
.B %s
Prints a string of characters

.TP
.B %d, %i
Prints a decimal integer

.TP
.B %%
Prints a literal percent sign

.SH RETURN VALUES
If successful, the number of characters printed is returned.
.br
If an output error is encountered, -1 is returned.

.SH AUTHOR
Written by Anthonia Ifoeze and Max Brook
Custom printf group project
